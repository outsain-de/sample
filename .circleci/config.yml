version: 2
jobs:
  build:
    docker:
      - image: ubuntu:16.04

    steps:
      - checkout

      - run:
          name: Install Requirements
          command: apt update && apt upgrade -y && apt install make -y
          
      - run: 
          name: run building test docker image
          command: make -c service-1 
